# API and health routes — read by nginx-microservice deploy-smart.sh from this repo.
# Ensures /health and /api/ (including email-triage) are explicitly allowed when config is regenerated.
# One route per line; deploy-smart.sh updates the service registry and blue-green config is generated from it.

/
/health
/api/
/api/email-triage/
